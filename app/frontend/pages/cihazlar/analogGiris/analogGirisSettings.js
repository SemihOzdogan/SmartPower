import React, { Component } from 'react';
import { View, Text } from 'react-native';

class analogGirisSettings extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> analogGirisSettings </Text>
      </View>
    );
  }
}

export default analogGirisSettings;
