import React, { Component } from 'react';
import { View, Text } from 'react-native';

class sayiciSettings extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> sayiciSettings </Text>
      </View>
    );
  }
}

export default sayiciSettings;
